pip install python-dotenv
pip freeze > requirements.txt
creamos en la raiz .env
MYSQL_HOSTNAME = localhost
MYSQL_USERNAME = root
MYSQL_PASSWORD = ''
MYSQL_DATABASE = database
MYSQL_PORT = 3306

en la carpeta config creamos settings.python
from dotenv import load_dotenv
import os
#sirve para leer variables de entorno
#Carga de rchivo .env a las variables de entorno
load_dotenv()
MYSQL_HOSTNAME = os.environ.get('MYSQL_HOSTNAME')
MYSQL_USERNAME = os.environ.get('MYSQL_USERNAME')
MYSQL_PASSWORD = os.environ.get('MYSQL_PASSWORD')
MYSQL_DATABASE = os.environ.get('MYSQL_DATABASE')
MYSQL_PORT = os.environ.get('MYSQL_PORT')

en la carpeta /


servidor de correo
creamos un archivo en raiz send_mail.py
from smtplib import SMTP
from email.message import EmailMessage
importamos librerias